fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
langgraph>=0.2,<0.3
langchain-core>=0.2.43,<0.3
# ^ pinned explicitly (not just left to install as langgraph's transitive
# dependency) so everyone gets the same tested version instead of whatever
# pip happens to resolve on a given day -- an untested newer/older version
# is exactly what caused an inconsistent warning-suppression result between
# environments during development.
openai>=1.51
numpy>=1.26
python-dotenv>=1.0
tenacity>=8.2
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
